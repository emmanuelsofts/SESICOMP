@model IEnumerable<WebApplication4.Models.Email>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email_type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created_on)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Updated_on)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email_type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created_on)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Updated_on)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Email_student">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Email_student">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Email_student">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
